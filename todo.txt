- Enteros: 
 -- Arreglar loop infinito si busco funcion(valor, ecuacion) con valor erroneo (exeq.txt)
    Capaz conviene limitar el Int,
 -- Cambiar a notacion Add NumToken NumToken, para handlear precedencia
      --- data EqToken =   Plus EqToken EqToken 
                           | Minus EqToken EqToken 
                           | NumTimes EqToken EqToken 
                           | NumDiv EqToken EqToken 
                           | VarNum String 
                           | Num Int deriving (Show, Eq)
      --- Esto implica cambiar el eval y el handleo de variables, aparte que ya no es mas una lista 
 -- Agregar precedencia parser
 -- Cambiar de int a float el Num y parsearlo

Debug:
   -- Mixear tipos de variable

Cleanup:
   -- Modularizar Logic
   -- Sacar seq
   -- Sacar VarN?
   -- Sacar () extras

General:
   -- Mejorar ux
   -- Documentar
   -- Comentar


Review:
- Enteros: 
 -- Ver que pasa y handlear cuando hago una query generica a una funcion con ecuacion
   Tira UndefVar, puede ser correcto
General:
   -- Limpiar gramatica (No se si es tan necesario ahora)
- Listas:
- Lexer: